# governor-mcp

Model Context Protocol surface for AI agents to read, create, and attest
against Governor runs.

| Directory | Status | Description |
|---|---|---|
| `python/` | Planned | To be migrated from the existing `governor-mcp/src/governor_mcp/server.py` tree |

Kept separate from the core CLI so that agent-facing tooling can ship and
version independently from the human-facing CLI.
