# governor SDKs

Thin HTTP client libraries.

| Directory | Status |
|---|---|
| `python/` | Planned. The existing `governor_mcp.client.GovernorClient` will be extracted here |
| `typescript/` | Planned |
| `go/` | Planned |

All SDKs target the same versioned OpenAPI surface defined in
[`spec/openapi/`](../spec).
